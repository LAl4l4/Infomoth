<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ReleaseBack.Back.mapper.MarketSignalMapper">
    <!-- Serialize background writers without blocking snapshot readers. -->
    <select id="lockInputHash" resultType="string">
        SELECT input_sha FROM market_signal_state WHERE id = 1 FOR UPDATE
    </select>
    <select id="findSignature" resultType="string">
        SELECT signature FROM market_signal_state WHERE id = 1
    </select>
    <select id="findPayload" resultType="string">
        SELECT payload FROM market_signal_state WHERE id = 1
    </select>
    <update id="updateInputHash">
        UPDATE market_signal_state SET input_sha = #{hash} WHERE id = 1
    </update>
    <update id="updateSnapshot">
        UPDATE market_signal_state SET signature = #{signature}, payload = #{payload} WHERE id = 1
    </update>
</mapper>
